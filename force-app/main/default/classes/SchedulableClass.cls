public with sharing class SchedulableClass implements Schedulable{
   
        public void execute(SchedulableContext sc) {
            //OppTaskBatch b = new OppTaskBatch();
            //database.executeBatch(b,40);
        }
    
}
